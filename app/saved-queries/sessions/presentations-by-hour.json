{
	"name": "Presentations by Hour",
	"description": "Finds presentations that occured at a particular class hour, optionally filtering by x-hours.",
	"query": "SELECT * FROM presentations WHERE classHour = ? and xHour = ?;",
	"parameters": [{
						"name": "Class Hour",
						"type": "select",
						"required": true,
						"options": ["9L", "10", "11", "12", "2", "10A", "2A"]
					},
					{
						"name": "X-Hour",
						"type": "bool",
						"required": false
					}]
}
        
        